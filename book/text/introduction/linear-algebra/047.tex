\subsection{Array{Array{Float64, x},1}をArray{Float64, x+1}に変換}numpyでは\jl{array([matrix for i in range()])}などを用いると，1次元配列のリストを2次元配列に変換できた．Juliaでも同様にする場合は\jl{hcat(...)}や\jl{cat(...)}を用いる．\jl{...}はsplat operatorと呼ばれる．